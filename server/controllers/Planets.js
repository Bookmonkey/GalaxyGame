const Planets = {
  create: () => {
    // randomize option of planets
    // based on planet type get total building slots   
  },

  destroy: (planetId, planetName) => {
    // 
  },

  rename: (planetId, newPlanetName) => {
    
  },

  // 
  upgradeBuilding: (planetId, building) => {
    // this.addToQueue();
  },
  buildFleet: (planetId) => {},
  buildDefence: (planetId) => {},


  getAllQueues: () => { },
  
  getQueue: () => { },
  addToQueue: () => { },
  removeFromQueue: () => { },
}

module.exports = Planets